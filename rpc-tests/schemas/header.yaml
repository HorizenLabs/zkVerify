Header:
  title: Block Header
  type: object
  properties:
    parentHash:
      $ref: '#/components/schemas/hash32'
    number:
      $ref: '#/components/schemas/uint'
    stateRoot:
      $ref: '#/components/schemas/hash32'
    extrinsicsRoot:
      $ref: '#/components/schemas/hash32'
    digest:
      type: object
      properties:
        logs:
          type: array
          items:
            $ref: '#/components/schemas/unknownHex'